The app's button — red primary, blue secondary, white quiet.

```jsx
<Button onClick={build} disabled={count === 0}>Build photo pages</Button>
<Button variant="secondary" size="sm">Suggest captions</Button>
```

Labels are verb phrases in sentence case ("Make the job", "Save these sections"). Never "Submit" or "OK".
