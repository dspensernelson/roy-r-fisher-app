Word-based on/off indicator for a settings card.

```jsx
<Lamp state={keySet ? "on" : "off"} />
```

A word rather than a coloured dot alone, so the state is readable without decoding a colour. Pairs with a heading.
