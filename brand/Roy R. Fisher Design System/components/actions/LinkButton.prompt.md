Link-styled button for secondary and reversible actions.

```jsx
<LinkButton onClick={cancel}>Cancel</LinkButton>
```

Cancel is always a LinkButton, never a Button. Destructive actions ("Remove it") also use it, in link blue — the app has no red destructive button.
