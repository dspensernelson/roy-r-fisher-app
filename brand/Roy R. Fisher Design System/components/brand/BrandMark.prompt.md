The firm's mark as a raster image — use wherever a logo goes; there is no vector.

```jsx
<BrandMark src="../../assets/logo/rrf-mark.png" height={40} />
```

Only `src` and `height` vary. Do not tint, rotate, or reconstruct the mark in SVG.
