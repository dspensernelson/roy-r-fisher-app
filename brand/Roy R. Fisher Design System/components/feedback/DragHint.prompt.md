Transient bottom-centre pill during a drag.

```jsx
{dragging && <DragHint>Drop to add these photos to the job</DragHint>}
```

The only fixed-position element besides the overlay. Results go in a `Banner` instead, so a message that matters stays on the screen it belongs to.
