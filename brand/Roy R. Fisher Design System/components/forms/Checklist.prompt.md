Multi-column checklist, normally inside a `<Panel>`.

```jsx
<Panel><Checklist items={sections} checked={picked} onToggle={toggle} /></Panel>
```

Sections stay in print order — never alphabetise. Sometimes-applicable sections start unchecked so the user decides.
