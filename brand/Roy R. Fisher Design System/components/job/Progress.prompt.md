The report's completeness, said in figures and words.

```jsx
<Progress ready={9} total={16} />
```

Goes in `ScreenHead`'s `below` slot. Tabular figures; no percentage, no ring, no bar — the bar only exists on JobCard where there is no room for a sentence.
