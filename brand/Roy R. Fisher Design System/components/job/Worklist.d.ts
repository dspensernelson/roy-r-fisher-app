export interface WorklistProps {
  /** Uppercase group heading, e.g. "You can do these now". */
  title?: string;
  children?: React.ReactNode;
}
export declare function Worklist(props: WorklistProps): JSX.Element;
