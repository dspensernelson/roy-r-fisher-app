export interface BandsProps {
  /** The wider column (1.45fr) — long wrapping lists. */
  left: React.ReactNode;
  /** The narrower column (1fr) — short rows. */
  right: React.ReactNode;
}
export declare function Bands(props: BandsProps): JSX.Element;
