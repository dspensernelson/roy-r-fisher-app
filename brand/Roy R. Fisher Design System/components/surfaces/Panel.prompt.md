Paper card grouping form fields and checklists under a ruled label.

```jsx
<Panel label="Needed to start"><div className="rrf-fields">{fields}</div></Panel>
```

Group by urgency, not by topic: "Needed to start" then "Can wait".
