A route choice presented before a form — white card, red top rule.

```jsx
<RoadCard title="Type it in" body="The address, what kind of property, and what kind of appraisal. About a minute." onClick={typeIt} />
```

Body copy says how long it takes and what it asks for. Use `soon` rather than removing an unbuilt road.
