# Check runs

One line per run, newest first. This is the record that a run happened and
what it found. The detail beyond that, notes on individual checks, lives in
the summary handed to the builder at the time.

---

**2026-09-04 — v0.6.6 — full set (11)**
9 passed, 2 failed, 1 not completed.
Failed: Check 8 (app does not come to the front on start), Check 9 ("Show the
log" opens a folder, not the log).
Not completed: Check 11 (update install) — needs a re-run.
